'use client';

import Settings from '@/components/Settings';

/**
 * Settings Page
 * Main settings page with tabs for profile, account, domains, etc.
 */

export default function SettingsPage() {
  return <Settings />;
}
